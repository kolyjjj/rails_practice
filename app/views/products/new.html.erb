This is products new page

<%= form_for :product, url: products_path do |f| %>
    <% if @product.errors.any? %>
        <div id="error_explanation">
            <h2><%= pluralize(@product.errors.count, "error") %> 个错误:</h2>
            <ul>
                <% @product.errors.each_with_index do |msg, i| %>
                    <li><%= msg[1] %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <p>
        <%= f.label :title, t('.name') %><br>
        <%= f.text_field :title %>
    </p>
    <p>
        <%= f.label :description, t('.description') %><br>
        <%= f.text_area :description %>
    </p>
    <p>
        <%= f.label :image_url, t('.image_url') %><br>
        <%= f.text_field :image_url %>
    </p>
    <p>
        <%= f.label :price, t('.price') %><br>
        <%= f.text_field :price %>
    </p>
    <p>
        <%= f.label :tags, t('.tags') %><br>
        <%= f.text_field :tags %>
    </p>

    <p>
        <%= f.submit t('.submit') %>
    </p>

<% end %>